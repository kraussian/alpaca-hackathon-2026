# Claude Code

Project guidance is maintained in `AGENTS.md` and imported below so it stays in context. The user's global preferences in `~/.claude/CLAUDE.md` still apply (PowerShell, `uv`/`pnpm`, terse, no em-dashes, no branch creation, no push without confirmation).

@AGENTS.md
