# Claude Code

Project guidance is maintained in `AGENTS.md` and imported below so it stays in context. The user's global preferences in `~/.claude/CLAUDE.md` still apply (`uv`/`pnpm`, terse, no em-dashes, no branch creation, no push without confirmation). Sessions run on a Linux VPS under bash, so shell snippets are bash.

@AGENTS.md
